# Videos de plantillas Remotion

Coloca aqui los previews exportados de cada composicion para que el backend los publique en la web.

Nombres esperados:

- `SalesPresentation.mp4`
- `TopScorersChart.mp4`
- `Scene.mp4`

Tambien se aceptan `.webm` y `.mov`. El nombre base debe coincidir con el `compositionId` definido en `remotion/src/Root.tsx`.
